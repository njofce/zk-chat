import { jest, test, expect, describe, beforeAll, beforeEach } from '@jest/globals'

describe('Test', () => {

    beforeAll(async () => {

    });

    beforeEach(async () => {

    });

    test('init', async () => {
        expect(true).toBeTruthy();
    });

});